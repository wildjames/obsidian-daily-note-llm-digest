{
  "name": "daily-notes-digest",
  "version": "1.0.0",
  "description": "Obsidian plugin to summarize daily notes with an LLM",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "release": "npm run build && node scripts/release.mjs"
  },
  "author": "Jim Wild",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.2.0",
    "esbuild": "^0.28.2",
    "obsidian": "^1.13.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "tslib": "^2.8.1"
  }
}
